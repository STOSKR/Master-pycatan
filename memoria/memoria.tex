\documentclass[10pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{amsmath}
\usepackage{float}
\usepackage{enumitem}
\geometry{margin=1.8cm}
\setlist[itemize]{noitemsep,topsep=2pt,leftmargin=*}
\setlist[enumerate]{noitemsep,topsep=2pt,leftmargin=*}
\renewcommand{\arraystretch}{1.08}

\title{Agentes heurísticos y modelos de lenguaje para PyCatan}
\author{Shiyi Cheng}
\date{Abril de 2026}

\begin{document}
\maketitle

\section{Objetivo}
El objetivo de la práctica es diseñar, implementar y evaluar un agente inteligente para el simulador PyCatan. El trabajo se divide en dos partes. La primera desarrolla un agente heurístico clásico y lo compara con agentes \textit{random} y agentes estándar. La segunda integra modelos de lenguaje (LLM) en decisiones concretas del agente para estudiar si son viables como política de decisión dentro de una simulación multiagente.

La aportación principal es doble: por un lado, un agente heurístico incremental con rendimiento claramente superior al aleatorio; por otro, una infraestructura experimental para comparar proveedores LLM, medir \textit{fallback}, latencia y calidad de decisión, y separar los resultados válidos de aquellos contaminados por decisiones de respaldo.

\section{Diseño del agente heurístico}
El agente heurístico se construyó de forma incremental, activando mejoras por bloques de decisión. Esta estructura permitió medir qué parte del comportamiento aporta más rendimiento. Las decisiones principales cubiertas son:

\begin{itemize}
\item \textbf{Colocación inicial}: evaluación de nodos por producción esperada, diversidad de recursos y calidad de la carretera inicial.
\item \textbf{Construcción}: priorización dinámica de ciudad, pueblo, carretera y carta de desarrollo según recursos disponibles y estado del tablero.
\item \textbf{Comercio}: uso de intercambios con la banca cuando permiten completar recursos críticos para el objetivo actual.
\item \textbf{Ladrón y cartas}: reglas simples para no desperdiciar acciones y mantener una política estable.
\end{itemize}

La heurística de nodos combina la probabilidad del dado con la variedad de recursos. En Catan, producir de forma regular y no depender de un único recurso suele ser más importante que maximizar una casilla aislada. Para la construcción, el agente intenta convertir recursos en puntos de victoria de forma temprana: primero ciudades si existen pueblos valiosos, después pueblos, carreteras y cartas. La carretera se valora por su capacidad de abrir nuevos nodos útiles.

\section{Infraestructura experimental}
Se refactorizaron los benchmarks en un módulo común, \texttt{benchmark\_common.py}, con wrappers para \texttt{vs\_random} y \texttt{vs\_standard}. La infraestructura genera CSV homogéneos con partidas, victorias, \textit{winrate}, puntos medios, rango medio, decisiones LLM, \textit{fallback} y proveedor/modelo cuando aplica.

Los experimentos heurísticos se ejecutaron con semillas reproducibles. En la evaluación iterativa se usaron las semillas \texttt{42}, \texttt{31415} y \texttt{2026}, 25 partidas por posición contra random, es decir, 100 partidas por semilla. Para los experimentos LLM se utilizaron ejecuciones completas cuando el coste lo permitió y ejecuciones ligeras para los modelos locales.

\section{Resultados del agente heurístico}
La Tabla \ref{tab:iterativo} resume el experimento de ablación. La columna \textit{gate 70\%} indica si la configuración supera el requisito mínimo frente a agentes random.

\begin{table}[H]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Iteración & Media winrate & Desv. típica & Mínimo & Gate 70\% \\
\midrule
iter0 random baseline & 0.2733 & 0.0047 & 0.2700 & No \\
iter1 game start & 0.2800 & 0.0294 & 0.2500 & No \\
iter2 build phase & 0.8433 & 0.0499 & 0.7900 & Sí \\
iter3 commerce trade & 1.0000 & 0.0000 & 1.0000 & Sí \\
iter4 thief devcards & 1.0000 & 0.0000 & 1.0000 & Sí \\
\bottomrule
\end{tabular}
\caption{Resumen del pipeline iterativo frente a agentes random. Fuente: \texttt{results/iterative/benchmark\_iterative\_summary.csv}.}
\label{tab:iterativo}
\end{table}

El salto más importante aparece al activar \texttt{on\_build\_phase}. Esto confirma que, en este simulador, convertir recursos en construcciones de forma coherente es más decisivo que mejorar únicamente la colocación inicial. La fase de comercio termina de cerrar el comportamiento: al desbloquear intercambios orientados a objetivos, el agente alcanza 100\% de victorias contra random en las semillas del gate.

Frente a agentes estándar, la primera versión competitiva evaluada fue \texttt{iter2\_build\_phase}. Sus resultados aparecen en la Tabla \ref{tab:std}. Aunque ya supera ampliamente al random, el rendimiento frente a agentes más fuertes todavía no es dominante, lo que muestra que el comercio y los módulos posteriores no son cosméticos.

\begin{table}[H]
\centering
\small
\begin{tabular}{rrrrr}
\toprule
Seed & Partidas & Victorias & Winrate & Puntos medios \\
\midrule
42 & 500 & 203 & 0.4060 & 6.5140 \\
2026 & 500 & 180 & 0.3600 & 6.2820 \\
31415 & 500 & 209 & 0.4180 & 6.5680 \\
\bottomrule
\end{tabular}
\caption{Iteración 2 frente a agentes estándar. Fuente: CSV de la iteración 2 en \texttt{results/iterative}.}
\label{tab:std}
\end{table}

\section{Integración de LLM}
El agente LLM se implementó como una variante de \texttt{ShiyiHeuristicAgent}. El modelo se consulta en dos decisiones: \texttt{on\_game\_start} y \texttt{on\_build\_phase}. En cada llamada se envía el estado mínimo necesario, una lista indexada de acciones legales y una instrucción para devolver JSON con el campo \texttt{action\_index}. Si la respuesta es inválida, fuera de rango o no llega a tiempo, el sistema registra \textit{fallback} y usa la acción heurística.

Se diseñaron tres variantes de prompt: \texttt{compact\_json}, \texttt{resource\_focus} y \texttt{safe\_legal}. Los experimentos finales se concentraron en \texttt{compact\_json}, porque es el formato más corto y el que mejor encaja con el requisito de respuesta estructurada. Durante las pruebas locales se detectó un fallo importante: algunos modelos pequeños tendían a copiar el JSON de entrada o a confundir \texttt{action\_index} con \texttt{node\_id}. Para corregirlo, el prompt pasó a usar acciones explícitamente indexadas y la restricción de que \texttt{action\_index} no es ni \texttt{node\_id}, ni \texttt{road\_to}, ni tipo de construcción.

\section{Resultados LLM}
La Tabla \ref{tab:llm} recoge los resultados comparables disponibles. Se marca como inválido cualquier resultado con \textit{fallback} alto, porque en ese caso la política real no es el LLM, sino la heurística de respaldo.

\begin{table}[H]
\centering
\scriptsize
\begin{tabular}{llllrrrrl}
\toprule
Proveedor & Modelo & Benchmark & Prompt & Partidas & Winrate & Avg pts & Fallback & Uso \\
\midrule
Bedrock & nova-lite-v1 & random & compact & 100 & 1.0000 & 10.04 & 0.0000 & válido \\
Bedrock & nova-lite-v1 & standard & compact & 100 & 0.9100 & 9.75 & 0.0000 & válido \\
Bedrock & llama3.3-70b & random & compact & 100 & 1.0000 & 10.12 & 0.0002 & válido \\
Bedrock & llama3.3-70b & standard & compact & 100 & 0.9400 & 9.77 & 0.0002 & válido \\
UPV & llama-mini & random & compact & 100 & 1.0000 & 10.05 & 0.0143 & aceptable \\
UPV & llama-mini & standard & compact & 100 & 0.9000 & 9.63 & 0.0000 & válido \\
Ollama & qwen2.5:0.5b & random & compact & 100 & 0.8300 & 9.21 & 0.0000 & válido parcial \\
Ollama & qwen2.5:0.5b & standard & compact & 4 & 0.2500 & 4.75 & 0.0000 & smoke test \\
\bottomrule
\end{tabular}
\caption{Resultados LLM principales. Fuentes: \texttt{results/bedrock\_llm/20260422\_093854}, \texttt{results/upv\_llm/20260422\_120019} y \texttt{results/llm\_light/20260424\_124431}.}
\label{tab:llm}
\end{table}

También se obtuvieron resultados descartados. \texttt{mistral.mistral-7b-instruct-v0:2} en Bedrock registró \textit{fallback\_rate}=1.0, por lo que sus victorias no pueden atribuirse al modelo. En UPV, \texttt{phi4-mini:3.8b} presentó \textit{fallback} de 0.4288 contra random y 0.5751 contra estándar, de modo que tampoco es una configuración fiable. En Ollama, una ejecución antigua de \texttt{gemma2:9b} produjo 0.9745 de \textit{fallback}; después de corregir el prompt, las llamadas directas ya respondían JSON válido, pero no se completó un benchmark limpio. Estos descartes son importantes: si se ignora el \textit{fallback}, el CSV puede mostrar victorias que realmente pertenecen al agente heurístico.

\section{Análisis de modelos}
Los modelos de Bedrock fueron los más estables. \texttt{nova-lite-v1} ofreció buen rendimiento sin \textit{fallback} y \texttt{llama3.3-70b} obtuvo el mejor resultado frente a agentes estándar. UPV \texttt{llama-mini} también fue utilizable, aunque con un pequeño \textit{fallback} contra random. En local se detectaron dos modelos mediante Ollama: \texttt{qwen2.5:0.5b} y \texttt{gemma2:9b}. El modelo pequeño, ejecutado en local con una AMD 6600 XT, consiguió completar 100 partidas contra random sin \textit{fallback}, pero su rendimiento fue peor que Bedrock: 0.83 de \textit{winrate} y 9.21 puntos medios. Esto sugiere que responder JSON válido no garantiza buena política.

La latencia local de \texttt{qwen2.5:0.5b} fue baja para un LLM: 488 ms medios por decisión en la ejecución completa contra random, con 8847 decisiones. Aun así, el coste total fue elevado porque se consulta el modelo muchas veces durante la partida. Por ello, una integración práctica debería reservar los LLM para decisiones de alto impacto o usarlos para generar heurísticas, no necesariamente para cada paso frecuente.

\section{Viabilidad de los LLM en PyCatan}
Los LLM pueden integrarse técnicamente en PyCatan, pero presentan tres riesgos. Primero, la salida estructurada no está garantizada: incluso con temperatura baja, algunos modelos devuelven índices fuera de rango o texto adicional. Segundo, la latencia multiplica el tiempo de benchmark. Tercero, si se permite \textit{fallback} silencioso, se sobreestima la calidad del modelo.

La conclusión experimental es que los LLM grandes y bien servidos funcionan como política aproximada para decisiones acotadas, especialmente cuando reciben una lista de acciones legales. Sin embargo, para un agente competitivo, la heurística sigue siendo más barata, reproducible y fácil de depurar. El uso más razonable del LLM sería como apoyo en decisiones estratégicas poco frecuentes, generación de explicaciones o exploración de variantes heurísticas, no como sustituto completo de la política del agente.

\section{Uso de herramientas de IA}
Durante el desarrollo se usaron asistentes de programación basados en IA para refactorizar benchmarks, diseñar el pipeline experimental, implementar el motor LLM, depurar errores de formato JSON y redactar esta memoria. Su utilidad principal fue acelerar tareas de integración y análisis. La limitación más clara apareció precisamente en la experimentación LLM: una salida aparentemente correcta puede esconder \textit{fallback}, por lo que fue necesario validar con CSV, métricas y pruebas manuales.

\section{Conclusiones}
\begin{enumerate}
\item El agente heurístico supera ampliamente el objetivo de 70\% frente a random desde la iteración de construcción, y alcanza 100\% al añadir comercio en las semillas evaluadas.
\item La ablación muestra que \texttt{on\_build\_phase} es la mejora más decisiva; el comercio consolida el rendimiento.
\item La integración LLM funciona en dos decisiones reales del agente: colocación inicial y construcción.
\item Bedrock \texttt{nova-lite-v1} y \texttt{llama3.3-70b} son las configuraciones más sólidas entre las evaluadas.
\item Ollama local es viable técnicamente, pero los modelos pequeños requieren prompts muy estrictos y aun así ofrecen menor calidad de decisión.
\item El \textit{fallback} debe tratarse como métrica crítica. Los resultados con \textit{fallback} alto no son válidos para comparar modelos.
\end{enumerate}

\end{document}
